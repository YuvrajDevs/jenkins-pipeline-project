{
  "name": "jenkins-pipeline-project",
  "version": "1.0.0",
  "description": "A Node.js app for a Jenkins Pipeline project.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Tests passed successfully!\" && exit 0"
  },
  "dependencies": {
    "express": "4.18.2"
  }
}
